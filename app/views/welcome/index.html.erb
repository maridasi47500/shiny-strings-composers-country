<%@country=Country.where(name: ["Italy", "France", "Russian Federation", "Germany", "Austria"])%>
<h2>choisis le pays d'une musique que tu joues à l'instrument :</h2>
<div class="row row-cols-1 row-cols-md-2 g-4">
<%@country.each do |country|%>

  <div class="col">
    <div class="card">
      <img src="..." class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title"><%=link_to country.name, "/users/sign_up?country_id=#{country.id}"%></h5>
        <p class="card-text"></p>
      </div>
    </div>
  </div>

<%end%>
</div>

